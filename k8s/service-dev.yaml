apiVersion: v1
kind: Service
metadata:
  name: front
  namespace: kwt-dev
spec:
  selector:
    project: kwt
    app: front
    env: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP